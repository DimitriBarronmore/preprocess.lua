#---
# rss = require("/lua/rss")
# data = {
# 	feedURL = "https://example.com/rss",
# 	title = "Preprocessor Test Feed",
# 	link = "https://example.com",
# 	description = "Test feed.",
# 	posts = {}
# }
# local blog = tree["/blog"]
# for k, post in ipairs(blog:_lsfiles()) do
#	if post ~= "rss.lua" and post ~= "rss.xml" then
#		post = blog[post]
#		local postdata = {}
#		postdata.title = post.metadata.title
#	 	postdata.description = post.metadata.description or ""
#		postdata.pubdate = post.metadata.date
#	 	postdata.link = data.link .. post.fullname
#	 	data.posts[#data.posts + 1] = postdata
#	end
# end
# table.sort(data.posts, function(a, b) return os.time(rss.dashdateToTimetable(a.pubdate)) > os.time(rss.dashdateToTimetable(b.pubdate)) end)
$(rss.exportRSS(data))